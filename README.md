# Movie software workspace

This repository contains two independent Vite applications:

- `creative-studio/` — Logfood, the project dashboard and screenplay workspace
- `chromaclick/` — ChromaClick, the on-set chroma key monitor

Run either application from its directory with `npm run dev`.
